CREATE TABLE history (
	row INTEGER PRIMARY KEY,
	filename VARCHAR,
	title VARCHAR,
	artist VARCHAR,
	deck INTEGER,
	starttime INTEGER,
	endtime INTEGER,
	played INTEGER,
	updatedAt INTEGER,
	playtime INTEGER,
	length VARCHAR,
	album VARCHAR,
	fullpath TEXT
);